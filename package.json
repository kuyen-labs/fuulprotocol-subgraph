{
  "name": "fuulfactory",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "graph:auth": "cross-var graph auth --product hosted-service $GRAPH_AUTH_TOKEN",
    "deploy:hosted": "graph deploy --product hosted-service fuul-app/fuul-protocol-subgraph-stg",
    "create-local": "graph create --node http://localhost:8020/ fuulfactory",
    "remove-local": "graph remove --node http://localhost:8020/ fuulfactory",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fuulfactory",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "cross-var": "^1.1.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
